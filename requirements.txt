# Python requirements for churchsuite.py

Flask >= 3.1.3
requests >= 2.32.3
requests-oauthlib >= 2.0.0
python-docx >= 1.2.0
pathvalidate >= 3.3.1

# Required only when deployed on Google App Engine
gunicorn >= 25.3.0

# Only required if get_google_secret() is used to fetch a value from Google Secret Manager
# google-cloud-secret-manager >= 2.27.0
